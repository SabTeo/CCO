Usr: ltw, Db: ccodb, pw:snap

Modifiche:

ALTER TABLE users DROP CONSTRAINT users_pkey;
ALTER TABLE users ADD PRIMARY KEY (nome);
ALTER TABLE users ADD UNIQUE (email);
ALTER TABLE users ALTER COLUMN paswd TYPE varchar(256);
ALTER TABLE users ALTER COLUMN email TYPE varchar(64);

Table Users(
    email varchar(64), 
    nome varchar(16),
    paswd varchar(256) not null,
    valuta int default 200,
    lastgift date,
    
    primary_key(nome)
    key(email)
)


